
            // while (abs(nr - nl) > 1) {
            //     i32 smid = (nl + nr) / 2;
            //     (pow(p[smid], 2) < mid ? nl : nr) = smid;
            // }
            // countn = (countn + nl) / 2;