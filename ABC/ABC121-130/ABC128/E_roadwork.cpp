#include<bits/stdc++.h>
using namespace std;

int main() {
    int n, q;
    cin >> n >> q;
    vector<pair<int64_t, int64_t>> st(n);
    vector<int64_t> x(n);
    for (int i=0; i<n; i++) {
        cin >> st[i].first >> st[i].second >> x[i];
    }
    vector<int64_t> d(q);
    for (auto& e : d) cin >> e;

    for (int i=0; i<n; i++) {
        
    }
}